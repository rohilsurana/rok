# HTTP Proxy

A small web application to get POST requests and encode the whole http request into a protobuf message and push it into Kafka.

### Dependencies

- HTTP Server - fasthttp
- Config lib - cleanenv
